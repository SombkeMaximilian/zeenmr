[package]
name = "benches"
version = "0.0.0"
publish = false
edition = "2021"
license = "MIT"

[dependencies]
zeenmr = { path = "../zeenmr", features = ["full"] }
criterion = "0.5.1"

[lints]
workspace = true

[[bench]]
name = "deconvoluter"
path = "deconvoluter.rs"
harness = false
